comments:
  allowedFields: ["name", "comment" , "tag" , "poster"]
  branch: "gh-pages"
  commitMessage: "Add {fileds.name}"
  filename: "Add Staticman {fields.poster}  {fileds.name}"
  format: "yml"
  moderation: true
  path: "_data/comments"
