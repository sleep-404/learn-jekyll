comments:
  allowedFields: ["name", "comment" , "tag" , "poster"]
  branch: "gh-pages"
  commitMessage: "Add {fields.name}"
  filename: "Add Staticman {fields.poster}"
  format: "yml"
  moderation: true
  path: "_data/comments"
